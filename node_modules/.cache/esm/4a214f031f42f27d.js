'use strict';;let yargs,nameColor,calculateCols;_1f4‍.w("yargs",[["default",["yargs"],function(v){yargs=v}]]);_1f4‍.w("./commands/name-color",[["default",["nameColor"],function(v){nameColor=v}]]);_1f4‍.w("./commands/calculate-cols",[["default",["calculateCols"],function(v){calculateCols=v}]]);






const args = yargs.help('help')
	.command(
		[ 'name <color>' ],
		'get color name for color value',
		(argv) => {
			yargs.positional('color', {
				describe: 'color to name',
				type: 'string',
			});

			return yargs.option('color');
		},
		({ color }) => nameColor(color)
	)
	.command(
		[ 'cols', 'calculate-cols' ],
		'calculate options for number of columns given width and spacing',
		argv => yargs
				.option('availableWidth', {
					alias: [ 'w' ],
					default: 0,
				})
				.option('spacing', {
					alias: [ 's' ],
					default: 0,
				})
				.option('minColWidth', {
					default: 4,
				})
				.option('maxColWidth')
		,
		argv => calculateCols(argv)
	)
	.argv;
